{
  "name": "onceglance",
  "private": true,
  "version": "0.1.0",
  "description": "定影 Onceglance · AI 原生截图工具（Agent 视觉层）",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "node scripts/copy-once.mjs && tauri build",
    "copy-once": "node scripts/copy-once.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
