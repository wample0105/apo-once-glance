# 定影 Onceglance 主面板 UI/UX 评审报告

- **日期**：2026-09-26
- **评审范围**：主面板五个页面（首页 / AI / 通用 / AI 助手 / 诊断），对应 v0.2 当前未提交构建
- **评审方式**：源码核对 + 运行实例只读探针（CDP 9700，只读不改，探针用后即删）+ 用户提供的 5 张实机截图
- **基准**：桌面工具主流实践（状态可见性、导航密度、文案规范、空态设计、一致性启发式）

---

## 一、总体结论

主面板整体合格：信息架构清晰（首页=动作区+核心能力+最近记录三段式）、设置四 tab 分组合理、诊断页自检全绿、文案经上一轮收敛后符合「后果导向、一行封顶」。

**问题共 7 项：P0 ×1、P1 ×1、P2 ×3、P3 ×2。** 其中 P0 影响功能（AI 问图当前必然报错），P1 为首发即存在的显示缺陷，其余为打磨项。

---

## 二、问题清单

| 级别 | 问题 | 核实证据 | 建议修法 |
|---|---|---|---|
| **P0** | 首页「AI 能力已就绪」徽章误报：只查「有 Key」，不查默认角色是否配置。实机 `default_vision=None`，AI 问图调用链走到默认视觉模型时必报「未设置默认模型——请到 AI 页选择」，与徽章宣称矛盾 | 实机 settings.json：`default_text=pvn4v`、`default_vision=None`；配置内视觉模型 `glm-4.5v` 存在但未被指定为默认。判定代码 `ui/js/main.js:183`（`ready = profiles.some(p => p.has_key)`） | 徽章判定改为「文字+视觉双角色都有默认值才亮绿」；缺哪个就显示哪个能力不可用。可在保存配置时自动补任默认视觉角色（现有配置有视觉模型即可补）。**立即可用的临时解**：在 AI 页「默认视觉模型」下拉选一次智谱官方 |
| **P1** | 通用页「落盘目录」灰条从上线起就是空白：前端读 `s.save_root` 与 `s.save_dir_writable`，但 `get_settings` 返回的 Settings 序列化 JSON 里没有这两个字段（二者是 Rust 计算方法，不进 JSON）。「目录不可写变红」的防护同为死代码 | 探针实测灰条 `textContent=""`、宽高=0；代码位置 `ui/js/main.js:377`、`src-tauri/src/lib.rs:516`；git 考古确认 M1 Alpha 首提交即如此，非本轮改动引入 | `get_settings` 返回时补输出 `save_root`（实际落盘根目录）与 `save_dir_writable`（可写性）两个计算字段 |
| P2-1 | 「查看全部 ↓」的箭头字符 ↓ 在小字号下形似数字 1（用户截图即误读为「查看全部 1」） | 探针实证按钮文案为「查看全部 ↓」，非数字；属字形歧义 | 箭头换 chevron 图标，或直接去掉箭头只留文字 |
| P2-2 | 核心能力卡快捷键语义混排：贴图卡写键名「D」，翻译/问图卡写动作「松手即翻/松手即问」，同一列位置两种语义，扫读卡顿 | `ui/index.html:400/404/408` | 统一语义：要么全用键名（贴图 D、翻译/问图暂无全局热键可写「框选后」），要么全用动作说明 |
| P2-3 | 最近截图第二行两张卡片（152636/140912）顶部文字似溢出卡片边界 | 截图观察，**待实机复核**（可能是缩略图本身内容） | 实机确认后再定：样式问题改 overflow 裁剪，内容问题不处理 |
| P3-1 | AI 助手页「未接入」徽章为绿底红字（复用 `tagok` 类强改红字），绿底与红字信号矛盾；且「未接入」是可选状态而非错误 | `ui/js/main.js:517` | 改中性灰底灰字；「未接入」不该借用成功徽章的底色 |
| P3-2 | AI 页空态文案拗口：「还没有自定义模板——添加后会问图指令中出现」 | `ui/index.html` 指令模板节 | 改为「添加后会出现在问图快捷指令里」 |

---

## 三、做得好的（保持）

- 设置顶栏「返回 + 四 tab」替代侧栏：轻工具的正确导航密度，页面跳转零适配成本；
- 诊断页「不含任何截图内容与 OCR 文字」隐私声明：同类工具中的加分项；
- AI 助手页 8 个客户端接入引导路径清晰，三态（未接入/连接中/已接入）正确；
- 内联删除二次确认、窗口尺寸记忆、详情页 1:1 原图直出：细节到位；
- 首页 AI 卡双态（引导态/就绪态）思路正确，问题只在判定条件不全（见 P0）。

---

## 四、建议动手顺序

1. **P0 + P1 一起修**（都在后端字段/逻辑层，一次构建）：`get_settings` 补两个字段；首页徽章改双角色判定 + 保存配置自动补默认视觉角色；
2. **P2/P3 一次清掉**（全是小改）：箭头图标、快捷键语义统一、徽章中性色、空态文案；
3. P2-3 卡片溢出实机查证后单独定。

修完照例真机回归，不提交、等用户验收授权。

---

## 五、证据附录

- **探针记录**（运行实例，只读）：
  - `#recent-all` 文本 = 「查看全部 ↓」（排除数字误读实为字符字形问题）；
  - `#pathmeta` = 「共 100 张」（位于全量视图内，首页不可见）；
  - `#save-dir-chip` 文本 = ""、宽高 = 0。
- **settings.json 关键值**（无敏感字段，Key 只存系统凭据管理器）：`default_text=pvn4v`、`default_vision=None`、profile「智谱官方」text=glm-4-flash / vision=glm-4.5v、`save_dir` 未自定义（默认 Pictures\Onceglance）。
- **代码位置**：`ui/js/main.js:183`（就绪判定）、`ui/js/main.js:377`（save_root 读取）、`src-tauri/src/lib.rs:516`（get_settings）、`src-tauri/src/ai.rs:249`（ai_ask_region 默认角色解析）、`ui/index.html:400/404/408`（能力卡快捷键）。

---

**评审人**：UI Designer
**状态**：待用户确认修复范围后开工

---

# 修订一（2026-09-26 晚）：外部评审交叉核对

用户提供了另一份外部 AI 评审报告。逐条与代码事实核对后：**4 条与现状不符（对方基于截图臆断，未查代码）、7 条为有效补充、其余与我方结论重合**。以下只记差异与新增，重合项不重复。

## 一、对方论断与代码事实不符的（不采纳，证据如下）

| 对方论断 | 代码事实 | 结论 |
|---|---|---|
| 「删除模型配置**缺少二次确认**」 | 内联二次确认已实现（2026-09-25 P3-5 评审落地）：首击变「确认删除？」红 armed 态、3 秒无操作回落，`ui/js/main.js:873-886` | **不成立**。但对方引出的「默认模型删除后默认角色悬空」是真的——`prune_defaults` 只把 `default_text/vision` 置空**不迁移**，删默认配置后翻译/问图即报「未设置默认模型」。真需求=「删除默认模型时自动迁移默认值并告知」，采纳其建议本体 |
| 「测试连接**缺少异步反馈**，需要 loading 态+行内结果」 | 已实现：按钮禁用+「测试中…」+行内逐项「文字 ✓ 1983ms / 视觉 ✕ 原因」，`ui/js/main.js:845-869` | **不成立**（截图是静态按钮看不出动态行为，属截图评审的天然盲区） |
| 「快捷键『可改』**完全没有表达**」 | chip 已可点击录制：`title="点击改键"`+`cursor:pointer`+点击进录制态+冲突检测降级提示，`ui/js/main.js:388/396-399` | **不成立**。但「title 提示不可发现」的观察有部分价值——hover 才可见，可再加虚线边框等静态暗示，列为 P3 打磨 |
| 「指令模板空态写了『添加后会在问图指令中出现』是**做得好的**」 | 同一份文案在我方报告 P3-2 判为拗口（「会问图指令中出现」缺介词，读不通） | 双方引用的是同一句。**它有引导意识是对的，但语句本身病句**——保持我方 P3-2 修法：改「添加后会出现在问图快捷指令里」 |

## 二、对方有效的补充（采纳，并入修复清单）

| 编号 | 新增问题 | 证据核实 | 级别 |
|---|---|---|---|
| 补-1 | **品牌红与危险红未分离**：`--accent=#FF3B30`（品牌）与 `--error=#B3271C` 在 tokens 里实际是分开的，但「重新自检」主按钮用品牌红、删除按钮用 danger 描边，视觉上同为「红」，用户无法靠色相区分「强调」与「危险」 | `tokens.css:8/18` 两 token 已分，问题在**使用层**：诊断自检这类主动健康检查不该用红系主按钮 | P2，采纳建议本体：自检改中性/主色实心按钮；danger 红仅限删除/移除 |
| 补-2 | **默认模型删除无迁移**：见上表，删除默认配置后 `default_text/vision` 置空不迁移，翻译/问图直接失效且无引导 | `ai.rs:157-167 prune_defaults`、`src-tauri/src/ai.rs:131-139` | **P1**（功能韧性），与 P0 修法合并考虑：删默认时自动迁移到下一套有 Key 配置并 toast 告知 |
| 补-3 | **OCR 摘要与缩略图的层叠关系**：对方称「白色 OCR 文字叠印在缩略图上不可读」。实际结构是 OCR 摘要在卡片**缩略图下方的文字区**（`.cardocr`，非叠印）。但第二行两张卡片的溢出现象（我方 P2-3）说明该区域在特定内容下会顶出卡片——对方的「可读性关注」方向有效，实机复核时一并查 | `.cardocr` 在 `cardHtml` 文字区，非 overlay | P2-3 扩充：复核第二行卡片溢出时确认是否 OCR 行顶出 |
| 补-4 | **首页信息密度定位**：「最近截图应为主内容区、能力入口工具条化」——方向性建议成立，属产品定位级改动（展示页→效率页），涉及布局重构 | 非缺陷，是取舍。当前「动作区+能力+记录」三段式是新用户友好型；重度用户 90% 走热键不打开面板 | **P2-4（新）**：列为方向性 backlog，等 v0.2 提交后再议，不与本轮小修混做 |
| 补-5 | **MCP 长路径喧宾夺主 + 「互不相通」提示位置**：路径全展开确实压过状态；「同一家族的桌面端与 CLI 端配置互不相通」在列表**下方**（`ui/index.html:546`），重要的坑位提示应上移到列表上方 | 位置核实：syntaxhint 在 agent-reg 之后 | P3，采纳：路径截断显示（中段省略）、互不相通提示上移 |
| 补-6 | **诊断页缺总结态**：「一切正常 ✓ / N 项异常」大状态标识，列表降为明细——低成本高感知，采纳 | 现状 6 行绿勾需逐行扫 | P3，采纳 |
| 补-7 | **状态绿点无解释**：标题栏绿点已修过一半（`title="诊断状态"`+点击直达诊断+异常变色），但「绿色=正常」本身无文字说明，tooltip 只说「诊断状态」未说当前是什么状态 | `ui/index.html:353`、`main.js:74` | P3，采纳轻量版：tooltip 带状态文字（「诊断状态：正常，点击查看」） |

## 三、对方建议中与我方既有结论重合的（确认，不新增）

- 「未接入」徽章红字问题 = 我方 P3-1（对方定 P0 偏高——它不伤功能，是语义瑕疵）；
- 「AI / AI 助手」tab 命名边界 = 2026-09-25 P2 评审已议过（当时用户拍板保留现名），维持原决策，如需再改等用户发话；
- 快捷键 chip 可点击但暗示不足 = 我方核实后列 P3（见上表「不成立」第 3 条的保留部分）；
- 对比度：`--accent-text/--error=#B3271C` 深红 token 已存在且正文红色场景已用它（`tokens.css:8/18`），对方建议的「统一用 --accent-text」实际已落地；#FF3B30 只用于徽章底/图标/按钮面，不用于小号正文文字。绿色徽标（#0E6E4E on #E6F6EF）对比度约 5.4:1 达标。此项**基本已合规**，列为 P3 复核项而非缺陷。

## 四、修订后合并修复清单（待用户拍板）

- **第一批（功能）**：P0 徽章双角色判定+问图可用性明示；补-2 删默认模型自动迁移；P1 save_root 字段补输出
- **第二批（语义/文案小改）**：P2-1 箭头图标；P2-2 快捷键语义统一；补-1 自检按钮去红；P3-1 未接入徽章中性色；P3-2 空态文案
- **第三批（打磨，可延后）**：补-5 路径截断+提示上移；补-6 诊断总结态；补-7 绿点 tooltip；快捷键 chip 静态暗示；P2-3 卡片溢出实机复核
- **方向性 backlog**：补-4 首页「效率页」重构（v0.2 提交后专题议）

> **评审方法注记**：两份报告分歧的根源是评审方式——对方基于静态截图（看不出交互态、动态反馈、已实现的确认流），我方全部结论经源码+运行实例双核实。截图评审的四个误判（二次确认、异步反馈、改键、空态评价）均属此类。后续评审建议以「截图提线索、代码下定论」为流程。

**修订人**：UI Designer

---

# 修订二（2026-09-26 晚）：整体 UI/UX 改进空间评估（源码层第三轮）

用户问「整体 UI/UX 是否有改进空间」。本轮从设计系统视角对 `ui/index.html`、`css/tokens.css`、`js/main.js` 做源码级核对，结论：**有空间，但不在继续挑毛病，而在三个层面——定位层（主窗口角色）、功能层（修订一清单仍待修）、结构层（本轮新发现 3 条）**。

## 一、本轮新发现（源码实证）

| 编号 | 问题 | 证据 | 级别 | 修法 |
|---|---|---|---|---|
| 新-1 | **`<main id="content">` 重复嵌套**：index.html 339 行与 371 行各开一个同 id 的 main，全文只有一个 `</main>`（737 行），解析后形成嵌套双 main、重复 id。视觉上碰巧无碍（内层 main 无样式），但属无效 HTML：重复 id 违反规范，嵌套 main 使辅助技术的地标（landmark）树混乱 | `ui/index.html:339`、`ui/index.html:371`、`ui/index.html:737`；`main.js` 无 `#content` 引用，删除外层开标签无副作用 | P2 | 删除 339 行的 `<main id="content">` 开标签，让 341 行的设置顶栏成为 winbody 直接子元素（其 flex:none 已具备），371 行成为唯一 main；一行改动 |
| 新-2 | **历史网格键盘盲区**：卡片是 `div.card` + click，无 tabindex/role/键盘事件——纯键盘用户无法打开任何截图详情；卡上「打开/复制路径」按钮 `.cardacts` 默认 `opacity:0; pointer-events:none`，仅 hover 显现，tab 聚焦时不可见也不可点。「悬停不是唯一途径」未满足 | `ui/index.html`（.cardacts 样式 203-214）、`ui/js/main.js:235-276`（cardHtml/wireCards） | P2（桌面鼠标优先工具，实际影响收窄，但属低成本必修） | 卡片加 `tabindex="0"` + `role="button"` + Enter 打开；CSS 补 `.card:focus-within .cardacts { opacity:1; pointer-events:auto }` 两行 |
| 新-3 | **小字号下限偏低**：kindbadge 10px、cardmeta/hc-state/kbd 10.5px。桌面工具元信息字号业界下限约 11px，10.5px 在 96dpi 下扫读吃力 | `ui/index.html:194/198/141`、`tokens.css`（.kbd 11px） | P3 | 全局下限提到 11px，仅动 token 级两三处 |

另核（不立项）：搜索框仅 placeholder 无 label（P3 级，随新-2 顺手补 aria-label）；bridge-warn 红色警示语义可接受。

## 二、做得好的（源码确认，保持）

- tokens.css 令牌体系完整：语义色分角色（brand accent / error / success / warn 分立）、圆角 5 档规模、明暗两套（亮工作台 + 暗取景层独立 token 组）、`:focus-visible` 全局焦点环已落地；
- 「强调」与「危险」已分离（`.btn.solid` 中性实心 vs `.btn.danger` 红描边），修订一补-1 的方向此前已修；
- 空态有引导（「还没有截图，按 Alt+Shift+A 试一次」）、错误可见化（frontend-err 红条 + reportErr 通道）、窗口尺寸记忆排除详情页干扰——细节意识好。

## 三、整体改进空间分层（结论）

1. **定位层（最大空间，上轮已给方案 A/B/C）**：主窗口从「门面」转为「按需弹出」——关窗缩托盘默认化、首页改「最近截图优先」、引导内容只给首次用户。与修订一补-4（效率页 backlog）是同一件事，v0.2 提交后专题做。
2. **功能层（第一优先不变）**：修订一合并清单第一批（P0 徽章判定、补-2 删默认迁移、P1 save_root）仍是当前最高优先，未修。
3. **结构层（本轮新增，便宜）**：新-1 并入第二批（一行删除），新-2 并入第三批（几行 CSS + 事件），新-3 列入打磨。

## 四、合并后修复清单（修订二版，待拍板）

- **第一批（功能）**：不变（P0 + 补-2 + P1）
- **第二批（小改）**：原 5 项 + 新-1（重复 main）
- **第三批（打磨）**：原 5 项 + 新-2（键盘可达）+ 搜索框 aria-label；新-3 字号下限随第三批顺带
- **方向性 backlog**：补-4 / 定位层改造（不变）

**修订人**：UI Designer（星衡）
